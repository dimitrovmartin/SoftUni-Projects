function solve(first, second, third){
    lengthSum = first.length + second.length + third.length;
    averageLengthSum = Math.floor(lengthSum / 3);

    console.log(lengthSum);
    console.log(averageLengthSum);
}